package chapt_3

object test {
  def main(args: Array[String]): Unit = {
    println("all is working fine!")
  }
}


/**
  * trait: Used to define a new data type
  *     trait is abstract, may or may not contain implementations of methods
  */

/**
  * sealed: a sealed type can only be extended in the same file
  */

/**
  * def apply[A](as: A*): List[A] = ???
  *     is a variadic function, meaning it accepts zero or more arguments of type A
  */